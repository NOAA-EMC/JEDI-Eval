merge:
  # common
  cycle: '{{current_cycle}}'
  window begin: '{{window_begin}}'
  experiment: $(experiment)
  experiment_dir: $(experiment_dir)
  obs:
    # obs stage
    obs_dir: '{{obs_dir}}'
    observations: $(obs_list)
    obs_db: $(obs_db)
